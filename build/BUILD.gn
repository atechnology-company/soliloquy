# Build rules for V tooling

import("//build/v_rules.gni")

# Smoke test target for c2v tooling
v_object("c2v_tooling_smoke") {
  sources = [ "c2v_smoke_test.v" ]
  output_name = "c2v_tooling_smoke"
}

group("tests") {
  testonly = true
  deps = [ ":c2v_tooling_smoke" ]
}
