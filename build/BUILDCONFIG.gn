# Soliloquy Build Configuration

if (target_os == "") {
  target_os = "fuchsia"
}

if (target_cpu == "") {
  target_cpu = "arm64"
}

is_debug = true

# Toolchain
set_default_toolchain("//sdk/build/toolchain:fuchsia_arm64")
