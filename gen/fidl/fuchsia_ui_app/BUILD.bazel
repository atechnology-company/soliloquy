# Bazel build for fuchsia_ui_app

load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "fuchsia_ui_app",
    srcs = ["src/lib.rs"],
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        # TODO: Add Fuchsia SDK Rust dependencies for Bazel
    ],
)
