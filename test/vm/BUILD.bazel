load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "vm_test",
    srcs = ["vm_test.cc"],
    deps = [
        "//third_party/zircon_c/vm:zircon_c_vm",
        "@googletest//:gtest_main",
    ],
)

test_suite(
    name = "tests",
    tests = [":vm_test"],
)
